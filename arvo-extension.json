{
  "id": "alpaca",
  "name": "Alpaca",
  "version": "0.1.0",
  "description": "Alpaca bars and dividends, IEX and SIP, split-adjusted and total return, as a source plugin. Keys arrive with each call and are never held.",
  "contributes": {
    "providers": [
      {
        "id": "alpaca",
        "services": ["arvo.source.v1.Source"],
        "build": "cargo build --release",
        "run": "target/release/arvo-plugin-alpaca"
      }
    ]
  }
}
